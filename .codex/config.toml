# Minimal scoped Codex configuration for delegation-layer
# Strict sandboxing: read-only, no blanket approvals, no parallelism overrides.

[agents.reviewer]
description = "Scoped read-only code review, verification gate checks, and diff inspection."
config_file = "agents/reviewer.toml"

[agents.researcher]
description = "Scoped read-only codebase exploration, documentation analysis, and constraint checking."
config_file = "agents/researcher.toml"
