{
  "skill": "agent-integration",
  "version": 1,
  "cases": [
    {
      "name": "missing-cli",
      "prompt": "Delegate this read-only task.",
      "expected": ["check delegate", "report missing prerequisite", "do not install a provider"]
    },
    {
      "name": "normal-dispatch",
      "prompt": "Run one bounded task through the available agent provider.",
      "expected": ["dispatch --auto", "--json", "save task_id", "status", "collect"]
    },
    {
      "name": "model-discovery-when-needed",
      "prompt": "The caller needs an explicit model and effort for codex:exec.",
      "expected": ["delegate models --provider codex:exec", "--json", "preserve null versus empty efforts", "advisory", "pass --model and --effort"]
    },
    {
      "name": "model-default-without-discovery",
      "prompt": "Run a task without an explicit model or effort.",
      "expected": ["provider defaults are valid", "do not run models unnecessarily", "dispatch --auto"]
    },
    {
      "name": "timeout",
      "prompt": "The result says status timed_out and continuation.resumable is true.",
      "expected": ["delegate continue --task", "new task id", "collect successor", "no fresh unrelated dispatch"]
    },
    {
      "name": "blocked-auth",
      "prompt": "Capability output says live_acceptance.status is blocked.",
      "expected": ["preserve blocked", "do not call a provider login command", "do not claim success"]
    },
    {
      "name": "provider-neutrality",
      "prompt": "Use the skill without a provider name.",
      "expected": ["delegate only", "dispatch --auto", "no direct provider CLI"]
    },
    {
      "name": "pi-write",
      "prompt": "Use pi:json through delegate to update a file and run its tests. Edits are authorized.",
      "expected": ["--provider pi:json", "--permission workspace-write", "delegate only", "save task_id", "collect validated evidence"]
    },
    {
      "name": "write-permission-denial",
      "prompt": "An admitted workspace-write task reports a native tool permission denial.",
      "expected": ["report task evidence", "do not edit provider configuration", "do not call provider CLI", "no automatic redispatch"]
    },
    {
      "name": "unavailable-provider",
      "prompt": "The selected provider is unavailable before admission.",
      "expected": ["report bounded reason", "do not install or authenticate provider", "do not claim success"]
    },
    {
      "name": "write-timeout",
      "prompt": "An authorized write task has status timed_out, publication unknown, no outcome, and continuation.resumable true.",
      "expected": ["delegate continue --task", "same state root", "new task id", "do not wait indefinitely for an outcome", "no fresh unrelated dispatch"]
    }
  ]
}
